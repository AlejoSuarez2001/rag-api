# HTTP & API
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0

# Validation & settings
pydantic==2.7.1
pydantic-settings==2.2.1
PyJWT[crypto]==2.9.0

# Vector database
qdrant-client==1.9.1

# Cache / memory
redis==5.0.4

# Feedback persistence (PostgreSQL)
asyncpg==0.29.0

# ORM (modelado de esquema) + migraciones
# Las queries siguen en asyncpg; SQLModel es la fuente de verdad del esquema y
# Alembic versiona los cambios. psycopg2 es el driver sync que usa Alembic.
sqlmodel==0.0.22
alembic==1.13.2
psycopg2-binary==2.9.9

# Token counting
tiktoken==0.7.0

# Dev utilities
python-dotenv==1.0.1
